# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(assent STATIC 
  assent.c)

include(Tornado.cmake)
set_tornado(assent)

target_include_directories(assent PUBLIC ../include)


target_link_libraries(assent PUBLIC 
  transmute
  nimble-steps
  nimble-steps-serialize
  imprint)

